replicaCount: 2

image:
  repository: registry.gitlab.com/astle286/prophaze-machine-test
  tag: "v1.0.0"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 200m
    memory: 256Mi

  limits:
    cpu: 500m
    memory: 512Mi

livenessProbe:
  path: /actuator/health
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  path: /actuator/health
  initialDelaySeconds: 15
  periodSeconds: 5

serviceAccount:
  create: true
  name: ""

ingress:
  enabled: false
  className: nginx

  annotations: {}

  hosts:
    - host: order-service.local
      paths:
        - path: /
          pathType: Prefix

  tls: []

autoscaling:
  enabled: false